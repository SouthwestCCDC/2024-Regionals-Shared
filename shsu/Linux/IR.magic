# Do the magic to the input to get the good good output
awk '/pid=.{1,},/ {print $1" - "$2" - "$5" - "$6" - "$7}' | awk -F ',' '{split($0, arr, "-"); $pid=substr($2,5,length($2)); printf "%s %s %s %s %s", arr[1], arr[2], arr[3], arr[4], pid; system("xargs -0 < /proc/"$pid"/cmdline")}'